# Copie este arquivo para .env antes de subir o banco:
#   cp .env.example .env
# O .env não é versionado.

MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=bookshelf
MYSQL_USER=bookshelf
MYSQL_PASSWORD=bookshelf
